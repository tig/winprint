using System;
// WinPrint Test App
namespace WinPrint {
    static class Program {
        static void Main(string[] args) {
            if (args.Length > 0) {
            }
           Application.Run(new MainWindow());
        }
    }
}
