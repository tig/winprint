<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!-- See https://aka.ms/maui-publish-app-store#add-entitlements for more information about adding entitlements.-->
    <dict>
        <!-- App Sandbox is intentionally DISABLED. winprint is distributed via a Homebrew
             cask (Developer ID signed), not the Mac App Store, and it must read arbitrary
             user files passed on the command line — the sandbox blocks that, and without
             the files.user-selected entitlement it also makes the file picker silently
             do nothing. If Mac App Store distribution ever becomes a goal, re-enable
             com.apple.security.app-sandbox and add
             com.apple.security.files.user-selected.read-only (plus security-scoped
             bookmarks for CLI/recent files). -->
        <key>com.apple.security.app-sandbox</key>
        <false/>
        <key>com.apple.security.network.client</key>
        <true/>
    </dict>
</plist>

