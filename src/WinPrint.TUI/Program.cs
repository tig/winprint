// Placeholder entry point for the `wp` command-line front end.
//
// This proof-of-concept milestone only stands up the encapsulated editor views
// (MarginEditor and its bases) plus the ANSI-golden test infrastructure. The full
// Terminal.Gui.Cli host (bare-args => help, file/glob printing, --tui, --gui) is built
// in a later step; until then `wp` is not yet a usable command line.
return 0;
